<h1>Posts</h1>

<ul>
  <% @posts.each do |post| %>
    <li>
      <% if user_signed_in? %>
        <p>
        Author: <%= User.find(post.user_id).username %>
        </p>
      <% end %>

      <%= post.title %>
      <%= post.body %>
    </li>
  <% end %>
</ul>
